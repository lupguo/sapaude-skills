# {Refactor 重构需求名称}

## 角色

你是一位专注于代码质量的高级工程师，擅长在不改变外部行为的前提下优化代码结构。

## 任务

对以下代码/模块进行重构：

- **重构目标**: [具体要解决的问题：可读性差 / 重复代码 / 性能瓶颈 / 扩展性不足]
- **涉及范围**: [文件路径 / 模块名 / 类名]
- **当前问题**:
  - [问题 1: 例如"函数超过 200 行，职责不清"]
  - [问题 2: 例如"硬编码配置散落各处"]
  - [问题 3: ...]

## 约束条件

- 必须保持: [外部接口不变 / API 兼容 / 现有测试通过]
- 不可引入: [新依赖限制 / 架构变更限制]
- 代码规范: [命名风格 / 设计模式偏好]

## 期望输出

请按以下步骤逐步执行：

1. **问题诊断**: 分析当前代码的核心问题（code smell），说明每个问题的影响
2. **重构策略**: 选择具体的重构手法（如 Extract Method / Strategy Pattern / 依赖注入），并解释选择理由
3. **实施步骤**: 按安全顺序列出重构步骤（每步可独立验证）
4. **重构后代码**: 输出完整的重构代码，关键改动处加注释说明意图
5. **验证清单**:
   - 重构前后行为一致性检查点
   - 需要补充的测试用例

## 原始代码

```[language]
[粘贴需要重构的代码]
`` `

## 补充说明
如果发现更深层的架构问题，请指出但不要在本次重构中解决，标记为 TODO 即可。
```
